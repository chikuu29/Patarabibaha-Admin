import { FormatannualPipe } from './formatannual.pipe';

describe('FormatannualPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatannualPipe();
    expect(pipe).toBeTruthy();
  });
});
